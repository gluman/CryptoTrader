# Handoff: Claude Code → Hermes (2026-07-23)

**Контекст:** Claude Code подхватил оборвавшуюся сессию Hermes по оптимизации параметров
(V26, агент MiniMax-M3 упал на glm-5.1 `max_retries_exhausted` сразу после вопроса Босса).
Ниже — что сделано, выводы и открытые решения. Все проверки на реальных данных Bybit,
живом коде v7a и live-логах. Реальные деньги, mainnet — код live НЕ менялся без явного добра.

---

## 0. TL;DR для Hermes
- ❌ **V26 (+19.93%) — переобучение.** OOS/walk-forward не подтвердили эдж. Линия закрыта. НЕ применять к live.
- 🔴 **Засуха (0 сделок с 11.07) — это НЕ фильтры входа, а упавший LLM:** MiniMax-M3 отдаёт HTTP 429 `Token Plan usage limit reached (2056)` — кончились кредиты. Mode D = LLM-primary → тотальный HOLD.
- ✅ **Trend-фильтр (R25) оправдан:** counter-trend — худший бакет. Но механический edge отрицателен после комиссий в любом бакете.
- 🩹 **Починен баг лога** `19 ok, 0 errors` (маскировал 429). Теперь `0 ok, 19 failed — 19× llm_call_failed`.

---

## 1. V26 OOS-валидация → ЗАКРЫТА как переобучение

Hermes-скрипт `/tmp/v26_quick.py` выбирал лучшую из 45 комбо на ОДНОМ 30д-окне → in-sample cherry-pick.
`+19.93%` = **сумма per-trade %**, не доходность (≈ +0.063%/сделку = полоса комиссионного шума).

**Проверка (60д, split IS 66% / OOS 34%, per-pair декомпозиция):**
- V26 на full 60д: **exp ≈ −0.04%/сделку, Σ ≈ −25.8%** (649 сделок). IS(ранние 40д) = −38.8%, OOS(поздние 20д) = +12.9% → смена режима, не эдж.
- Walk-forward 3 фолда: непоследовательна (+5.4 / −5.1 / +18.0).
- Per-pair: только 2/19 пар в плюсе в ОБОИХ окнах (SOXL persistent, SUI крошечная). 12/19 убыточны в обоих.
- **AKEUSDT** (герой Hermes +22.82%) — IS НЕГАТИВНА, весь плюс = 20 сделок недавнего всплеска. Она одна даёт +21.3% из OOS +12.9% (остальные 18 пар = −8.4%).
- Отбор пар по IS-плюсу → OOS exp −0.008%, PF 0.97 (ноль). Предсказать сработавшую пару нельзя.

**Урок для Hermes:** любой grid-search, выбирающий «лучшую» на одном окне, → переобучение. Всегда split IS/OOS + walk-forward + per-trade expectancy (не Σ). Тот же паттерн, что закрытая ранее conf-формула.

## 2. Диагностика засухи (funnel 30д × 19 пар, живой стек v7a)

| Стадия | Осталось | % от баров |
|---|---|---|
| Всего баров | 163 026 | 100% |
| Час 08–15 UTC (v7-гейт 6–15 ∩ v6-session 8–20) | 47 880 | 29.4% |
| close-back за свип-уровень | 2 519 | 1.5% ⬅ главный механический срез |
| wick ≥ 0.4 | 653 | 0.4% |
| → PASS (кандидат до LLM) | 355 | ~12/день |

**Вывод:** механика даёт ~12 кандидатов/день — фильтры входа НЕ засуха.
**Реальная причина (live-логи, каждый скан 5м):**
```
M3 HTTP 429: "Token Plan usage limit reached ... (2056)"
⊘ <pair>: LLM HOLD conf=0.00 [llm_call_failed]   × все 19 пар
```
Mode D = LLM-primary → падение M3 = принудительный HOLD по всем парам. Данные свежие (age ~9м), regime=high.
**Действие Босса:** пополнить кредиты MiniMax ИЛИ вписать fallback-LLM (RuAPI Haiku / локальный qwen) в `mm_llm_gate`, чтобы Mode D деградировал грациозно, а не в тотальный HOLD.

## 3. Качество сигналов by trend-alignment (гипотеза Hermes)

312 сетапов по механике, помечены по 1h-тренду (SMA20/50, ±0.3%), симуляция R22 SL/TP + max_hold 15м, fee 0.11%:

| Alignment | n | WR% | PF | exp %/сделку |
|---|---|---|---|---|
| with-trend | 91 | 49.5 | 0.81 | −0.078 |
| neutral | 53 | 54.7 | 1.01 | +0.004 |
| against-trend | 168 | 43.5 | 0.66 | **−0.137** |
| LIVE (with+neutral) | 144 | 51.4 | 0.87 | **−0.048** |

**Выводы:**
1. ✅ Гипотеза Hermes подтверждается по направлению: against — худший. Блок R25 оправдан (улучшает live −0.096 → −0.048).
2. ❌ Даже отфильтрованный live-набор убыточен после комиссий (exp −0.048, PF 0.87). Механического edge нет ни в одном бакете.
3. ⚠️ Заявленные в R25-комменте «with-trend WR=87.5%, +4.45%» НЕ воспроизвелись (реально WR 49.5%, exp<0) — были на другой/малой выборке.

## 4. Изменения кода

- **Фикс лога** `clone5_multi_runner.py:581` — корректный подсчёт LLM-батча (`ok` = dict без `error`; отдельно `failed`/`errors` + топ-причина). Проверено на живом скане: `0 ok, 19 failed — 19× llm_call_failed`. **Uncommitted** (ждёт добра Босса на коммит).
- Прочие uncommitted правки в рабочем дереве (R24/R25 v6/v7, per_pair_params.json) — НЕ Claude, были до сессии.
- V26 к live НЕ применялась.

## 5. Состояние live (на 23.07 23:00)
- Исполнение: Hermes-cron `76260dbf8e29` Scan+Execute v7a (5m), 19 пар, $7/1x, min_conf 0.60.
- Баланс $21.94, 0 позиций, засуха продолжается (причина — M3 429).
- LLM: MiniMax-M3 через `api.minimax.io/anthropic` — **429 token-limit**.

## 6. Открытые решения для Босса
1. **LLM:** пополнить MiniMax или fallback в mm_llm_gate (без этого 0 сделок).
2. **Стратегия:** механика после комиссий в минусе; edge держится только на LLM (сейчас offline) → наращивать капитал/параллельные стратегии до доказанного live-edge смысла нет.
3. Коммитить ли фикс лога + R24/R25 правки.

## Артефакты
- Скрипты валидации: `scratchpad/v26_oos_fast.py`, `v26_perpair.py`, `drought_trend.py`; данные `ohlcv_60d.pkl`, `ohlcv_30d_ts.pkl`.
- Память Claude: `project_v26_optim_closed.md` (новая), `project_v7a_live.md` (обновлён диагноз засухи).
